import type { ModuleConfig } from '@/lib/modules/module-config';

/**
 * The __MODULE_NAME__ module's registry contract. Gated by the backend's
 * `__FEATURE_KEY__` entitlement; an org without it never sees the sidebar
 * entry or the route (`ModuleGuard` sends a direct visit to 403).
 *
 * Generated by `bun run scaffold:feature __MODULE_ID__`. The nav entry here
 * and `nav/metadata/__MODULE_ID__.meta.ts` describe the same route and the
 * same permission; `module.config.test.ts` keeps them in step.
 */
export const __MODULE_CAMEL__Module: ModuleConfig = {
  id: '__MODULE_ID__',
  entitlementFeatureKey: '__FEATURE_KEY__',
  nav: [
    {
      label: '__MODULE_NAME__',
      section: 'projects',
      path: '/users/dashboard/__MODULE_ID__',
      icon: 'Puzzle',
      requiredPermissions: ['__MODULE_ID__:read'],
    },
  ],
};
