name: Pull request

# Gate for changes entering `development`.

on:
  pull_request:
    branches: [development]

permissions:
  contents: read

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2

      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}
        run: bun install --frozen-lockfile

      # Reported, not blocking. The codebase currently has 93 lint errors that
      # predate this pipeline, so gating on them would stop all work rather
      # than improve anything. Flip continue-on-error off once the backlog is
      # cleared, and it becomes a real gate.
      - name: Lint
        run: bun run lint
        continue-on-error: true

      - name: Build
        run: bun run build
        env:
          NEXT_PUBLIC_API_URL: /api/v1

  secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      # This repository has a live NextAuth secret in its history. It is
      # already rotated, but the scan stops the next one.
      # The gitleaks GitHub Action requires a paid licence for organisations.
      # The scanner itself is MIT, so it is invoked directly.
      #
      # Only the commits this pull request adds are scanned. Gating on full
      # history would fail permanently on pre-existing matches that are example
      # data, and a check that is always red gets ignored.
      - name: Scan for committed secrets
        run: |
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz" \
            | tar -xz gitleaks
          ./gitleaks detect --source . --redact --no-banner \
            --log-opts="${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}"
